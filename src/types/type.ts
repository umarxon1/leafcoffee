export type Product = {
  id: number;
  img: string;        
  title: string;
  description: string;
  price: string;
};

export interface Testimonial {
  id: number;
  name: string;
  city: string;
  text: string;
  img: string;
}